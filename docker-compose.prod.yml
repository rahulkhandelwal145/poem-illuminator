services:
  web:
    ports:
      - "3001:80"
    restart: always
    environment:
      VITE_CF_ACCOUNT_ID: ${VITE_CF_ACCOUNT_ID:-}
      VITE_CF_TOKEN: ${VITE_CF_TOKEN:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
