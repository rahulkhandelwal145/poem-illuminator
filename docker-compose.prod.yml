services:
  web:
    ports:
      - "80:80"
    restart: always
    environment:
      CF_ACCOUNT_ID: ${CF_ACCOUNT_ID:-}
      CF_TOKEN: ${CF_TOKEN:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
