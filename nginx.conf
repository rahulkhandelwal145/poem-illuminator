server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Use Docker's internal DNS — defers resolution so nginx starts even
    # when sd-server isn't running
    resolver 127.0.0.11 valid=30s;

    # SPA fallback — all routes go to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy /local-generate → model server running on the host machine
    location /local-generate {
        set $upstream http://host.docker.internal:8083/generate;
        proxy_pass $upstream;
        proxy_set_header Content-Type application/json;
        proxy_read_timeout 300s;
    }
}
