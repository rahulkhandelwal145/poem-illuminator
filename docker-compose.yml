services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_HF_TOKEN: ${VITE_HF_TOKEN:-}
    ports:
      - "8080:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
