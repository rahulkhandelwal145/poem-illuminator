services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_HF_TOKEN: ${VITE_HF_TOKEN:-}
        VITE_GROQ_API_KEY: ${VITE_GROQ_API_KEY:-}
    ports:
      - "8080:80"
    environment:
      VITE_CF_ACCOUNT_ID: ${VITE_CF_ACCOUNT_ID:-}
      VITE_CF_TOKEN: ${VITE_CF_TOKEN:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
